/*package calculatorgame;

public class test {
    static int a = (int) (Math.random()*100);//mathrandom generate numbe rbetween 0 -0.99 so multiple by 10,100 depending on values needed. Also returns float but need integers
    static int b = (int) (Math.random()*100);

    public static void moduloQuestion(int a, int b) {
        int modulo1 = a % b;
        //ask user to add a  and b
        //Scanner scanner = new Scanner(System.in);
        System.out.println(" Calculate remainder of dividing " + a + " , " + b);

    }
}
*/

